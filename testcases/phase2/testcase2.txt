func int factorial (int n) {
    if(n eq 1){
        return 1;
    }
    return mul(n,call factorial(n-1));
}

func matrix matr_add (matrix A, matrix B){
    return add_matx(A, B);
}

func int main(){
	declr int a, b;
	declr string str;
	declr matrix A, B;
	call matr_add(A, B);
	expr a = exp(a, add(b, 5));
	declr string C;
	!! The following statement would give syntax error !!
	if(add_matx(A, B))
	{
		expr C = "Hello, World!\n";
	}
	return 0;
}
